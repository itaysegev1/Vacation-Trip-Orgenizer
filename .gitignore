# dependencies
node_modules
.pnp
.pnp.js

# build
dist
dist-ssr
*.local

# env / secrets — never commit real keys (.env.example IS kept as a template)
.env
.env.*
!.env.example

# credentials, private keys & service accounts
*.pem
*.key
*.p12
id_rsa*
id_ed25519*
*-firebase-adminsdk-*.json
serviceAccount*.json
google-services.json
GoogleService-Info.plist
secrets.*

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.claude/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# firebase
.firebase
firebase-debug.log
firestore-debug.log
# auto-generated from VITE_ALLOWED_EMAILS — contains real emails, kept out of the repo
firestore.rules
# ties the repo to a specific Firebase project (run `firebase use --add` locally)
.firebaserc
