// Shared Tailwind class strings so every page looks consistent.
export const input =
  'w-full rounded-2xl border border-line bg-white px-4 py-2.5 text-ink placeholder:text-ink-soft/50 outline-none transition focus:border-sakura focus:ring-2 focus:ring-sakura/30';

export const labelCls = 'mb-1.5 block text-sm font-semibold text-ink-soft';

export const btnPrimary =
  'inline-flex items-center justify-center gap-1.5 rounded-2xl bg-rose-deep px-5 py-2.5 font-semibold text-white shadow-soft transition active:scale-95 disabled:opacity-50';

export const btnGhost =
  'inline-flex items-center justify-center gap-1.5 rounded-2xl bg-petal px-4 py-2 font-semibold text-rose-deep transition active:scale-95';

// Elevated card surface — soft wide shadow + subtle white border (premium feel).
export const card = 'rounded-2xl border border-white/70 bg-white/85 p-4 shadow-soft';

// Same surface without padding (for cards that manage their own inner spacing).
export const surface = 'rounded-2xl border border-white/70 bg-white/85 shadow-soft';

export const sectionTitle = 'font-display text-2xl text-rose-deep';
